#include "main.h"

/**
 * _puts - function that prints a string, followed by a new line, to stdout
 * @str: string to print
 *
 * File: 3-puts.c
 * Auth: Zuhair Ahmed
 *
 */
void _puts(char *str)
{
	puts(str);
}
